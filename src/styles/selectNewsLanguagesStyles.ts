// Native import
import {StyleSheet} from 'react-native';

export const selectNewsLanguageComponentStyles =  StyleSheet.create({
  languageItem: {
    height: 50,
    justifyContent: 'center',
    paddingHorizontal: 10,
    borderRadius: 10,
  },
  languageContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
});
